spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:tcp://localhost/~/tx_exam
      username: test
      password: test
      driver-class-name: org.h2.Driver

  jpa:
    database: h2
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

#logging.level:
#    org.hibernate: debug